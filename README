Usage:

python3 info_pieces.py [EXAMPLE]

look in example.py for various names such as: xor, and, pwunq (all are case sensitive)

currently working on the 'disordered' version (where [1,0] == [0,1]) so it is a bit of a mess